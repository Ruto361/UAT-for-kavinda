{
  "name": "kavinda-uat-netlify",
  "version": "1.0.0",
  "description": "Kavinda UAT Acceptance Dashboard — Netlify edition (static HTML + Netlify Functions + Netlify Blobs)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
